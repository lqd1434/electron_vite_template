#生产环境变量
#路径皆相对于(asar中的main.js)

# asar 打包后结构如下
# -package.json
# -node_modules
# -dist
# -- main
# --- main.js
# --- prod.env
# -- preload
# -- render vite打包后 renderer目录

#electron 主进程preload文件路径
PRELOAD_PATH=../preload/index.cjs
#NODE_ENV环境变量
NODE_ENV=production
#主窗口路径
MAIN_WINDOW_PATH=../render/index.html
